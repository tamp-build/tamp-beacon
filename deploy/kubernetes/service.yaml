apiVersion: v1
kind: Service
metadata:
  name: tamp-beacon
  labels:
    app.kubernetes.io/name: tamp-beacon
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: tamp-beacon
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
