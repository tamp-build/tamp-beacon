nodeLinker: node-modules

# Yarn Berry uses Plug'n'Play by default. Beacon SPA pins node-modules because the
# react / tailwind ecosystem still has pockets of poor PnP support, and node-modules
# avoids friction with @vitejs/plugin-react's transitive deps.
